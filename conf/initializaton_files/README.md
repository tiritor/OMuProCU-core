# BFRT Initialization Scripts

In this directory, python scripts which should initialize different accelerator (esp. accelerator programs).

The name should be the name of the accelerator (and a suffix if needed),